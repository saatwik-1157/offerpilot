# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Local secrets / overrides (keep real keys out of git)
js/config.local.js

# Dependencies (only the optional RezForge live server needs these)
node_modules/
server/node_modules/
server/package-lock.json
.env
